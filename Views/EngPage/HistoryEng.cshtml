<div class="card">
<div class="container center-align">
    <br>
   <h5 class="left-align">
     Importnat Locations
   </h5>
   <hr>
<div class="row">
    <div class="col s12  left-align">
        <ul>
                  @foreach (var item in  ViewBag.history)
        {
            <li>
                <a href="@Url.Action("HistoryEng","Link",new{id=@item.Id})">
                       @item.TitleEng
                </a>
             
            </li>
      
        }
        </ul>
  
       
    </div>
</div>
</div>
</div>